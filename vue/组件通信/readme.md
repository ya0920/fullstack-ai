# 父子通信
1. 父组件通过 v-bind（:）绑定属性将自己的数据传递给子组件,子组件通过 defineProps 接收父组件绑定的属性(demo2)

# 子父通信
1. 子组件通过 defineEmits 定义一个事件，父组件通过 v-on（@）绑定事件监听子组件触发的事件（订阅行为），子组件通过 emit 发布事件，并传递参数（发布行为）(demo3)

# 兄弟通信
1. 仓库 vuex
2. pinia



