# 异步
1. js 单线程
2. 如果所有代码都同步执行，则执行效率大大降低

# async / await
1. async 添加在函数声明前，相当于在函数中，返回了一个 Promise 实例对象
2. await 后面接的是异步代码，但必须是 Promise 实例对象，会将后面的代码的执行结果返回出来

# promise
1. Promise.resolve() 会得到一个状态变更的 Promise 对象

# 红绿灯问题
1. 红灯 3 秒亮一次，黄灯 2 秒亮一次，绿灯 1 秒亮一次。如何让三个灯不断交替重复亮灯？（用 Promise 实现）

