# 运行方式
1. 确保你已经安装了 Node.js 和 npm / yarn。

2. 安装依赖
打开项目根目录（TodoList 文件夹）的终端，执行命令安装依赖：
```bash
npm install
# 或 yarn install（若使用 yarn 包管理器）
```
3. 启动开发服务器
依赖安装完成后，执行启动命令：
```bash
npm run dev
# 或 yarn dev
```
4. 访问
执行后，终端会输出本地访问地址（如 http://localhost:5173），在浏览器中打开该地址即可查看并使用 TodoList 项目。