# 异步
1. js 是单线程语言，一次只能干一件事（把耗时的代码挂起到了队列去，再去执行后面不耗时的代码）
（进程是计算机中正在运行的程序的实例；线程是进程内部的执行单元，是程序执行流的最小单元。一个进程可以包含多个线程）
（多线程执行效率快但是开销性能也多）
2. js 遇到需要耗时执行的代码，会将其先挂起，等到后续不耗时的代码执行完毕后，再回过头来执行耗时的代码

# 解决异步
1. 回调：回调地狱（代码嵌套过深，维护成本大，一旦出现问题很难排查

2. promise：借助 resolvs 修改实例对象的状态，从而导致 then 的执行，让我们可以讲异步处理成同步的顺序

