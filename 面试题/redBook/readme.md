1. 常见的错误类型
   - 变量为声明被使用
   - undefined 被拿去当函数，对象

2. 闭包

3. 计时器
   浏览器的倒计时像一个沙漏，同一批的计时器用的是同一个沙漏


3. vue 的数据流是单向的吗？为什么
   更好的控制数据流向，避免数据的混乱和不一致性。更好的测试和优化的代码

4. 判断元素是否在视图里
   - IntersectionObserver    监听元素是否在视图里
   - getBoundingClientRect   计算元素相对于视图上左的位置