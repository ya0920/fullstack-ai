# 直接循环渲染
 - 渲染线程会卡死，耗时很久

# 定时器
 - 页面滚动过快，会出现闪屏，白屏卡顿，因为定时器的执行时间是不固定的。

 - 定时器和屏幕刷新不同步

# requestAnimationFrame
 


# 虚拟列表
1. 通过可视区域的高度，和列表项的高度，计算出可视区域内的元素个数。
2. 滚动的时候，实时更新截取的数组的起始和结束下标 （多抛一倍的数据量为了让滚动过程中数据不会闪烁出现）。
3. 计算整个列表的高度，设置给容器 （借助 padding 来设置了列表的高度）。